# Final Radar 期末雷达

## 启动方式

### Mac
双击 `start.command`

### Windows
双击 `start.bat`

### 命令行
```bash
cd backend
pip install -r requirements.txt
uvicorn app.main:app --port 8000
# 浏览器打开 http://localhost:8000
```

> 需要电脑已安装 Python 3，首次启动会自动安装依赖。
